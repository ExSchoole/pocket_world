#parse("layout/macros.vm")
#macro( showbuilding $position )
    #set($building = false)
    #set($building = ${dto.getBuildings().get($position)})
    #if(  $building  )
    <div class="res_building_$building.getType() res_building_position res_building"></div>
    #else
    <div class="res_building_position res_building_empty"></div>
    #end
#end
#head("City Resources")

#@body()
<div class="col-md-7">
    <h1>${dto.NickName}'s city resources</h1>

    <div class="row">
        #foreach($position in [1,2,3,4])
            <div class="col-md-3">
                #showbuilding( $position )
            </div>
        #end

    </div>

    <div class="row">
        <div class="col-md-3">
            #foreach($position in [5,6])
                   #showbuilding( $position )
               #end
        </div>

        <div class="col-md-6">
            <div class="central_image res_building_position"></div>
        </div>

        <div class="col-md-3">
            #foreach($position in [7,8])
                 #showbuilding( $position )
               #end
        </div>

    </div>

    <div class="row">
        #foreach($position in [9,10,11,12])
            <div class="col-md-3">
                #showbuilding( $position )
            </div>
        #end
    </div>

</div>
#end